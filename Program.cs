public List<Adat> lista = new List<Adat>();

using(StreamReader sr = new StreamReader("lift.exe"))
{
    while (!sr.EndOfStream)
    {
        sr.ReadLine();
        // eddig jutottam
    }
}